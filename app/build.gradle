buildscript {
    repositories {
        mavenCentral()
        jcenter()
        maven {
            url "https://oss.sonatype.org/content/repositories/snapshots"
        }
        maven { url 'https://maven.fabric.io/public' }
    }

    dependencies {
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
        classpath 'io.fabric.tools:gradle:1.+'
    }
}

apply plugin: 'com.android.application'
apply plugin: 'kotlin-android'
apply plugin: 'com.neenbedankt.android-apt'
apply plugin: 'idea'
apply plugin: 'io.fabric'

repositories {
    maven { url 'https://maven.fabric.io/public' }
}

android {
    compileSdkVersion 'Google Inc.:Google APIs:23'
    buildToolsVersion "24.0.2"

    defaultConfig {
        applicationId "pl.rhinoonabus.slowka"
        minSdkVersion 16
        targetSdkVersion 24
        versionCode 1
        versionName "1.0"
        multiDexEnabled true
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    dexOptions {
        javaMaxHeapSize "4g"
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_7
        targetCompatibility JavaVersion.VERSION_1_7
    }
    sourceSets {
        main.java.srcDirs += 'src/main/kotlin'
    }
    lintOptions {
        abortOnError false
    }
}

dependencies {

    compile appcompat
    compile cirleProgressView
    compile crooper
    compile cardView
    compile dagger
    compile googleAnalytics
    compile (googleApiClientAndroid) {
        exclude module: 'httpclient'
    }
    compile (googleApiCLientGson) {
        exclude module: 'httpclient'
    }
    compile googleVisionApi
    compile kotlin
    compile kotterknife
    compile picasso
    compile processButton
    compile retrofit
    compile retrofitConverterGson
    compile retrofitRx
    compile roundCornerProgressBar
    compile rxjava
    compile rxjavaAndroid
    compile rxjavaAsyncUtils
    compile supportDesign
    compile timber
    compile 'com.android.support:multidex:1.0.1'
    compile('com.crashlytics.sdk.android:crashlytics:2.6.8@aar') {
        transitive = true;
    }
    compile('com.crashlytics.sdk.android:answers:1.3.13@aar') {
        transitive = true;
    }

    // during compilation only
    apt daggerCompiler
    provided javaxAnnotations
}
